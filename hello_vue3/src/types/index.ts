// 定义一个接口，接口内的属性name、id、age
// export 表示单个暴露
export interface SongComment {
    name: string,
    url: string,
    picurl: string,
    artistsname: string,
    avatarurl: string,
    nickname: string,
    content: string
}
