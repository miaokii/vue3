import { type ConfigProviderThemeVars } from "vant";

export const themeVars: ConfigProviderThemeVars = {
    primaryColor: '#366ded',
    tabbarHeight: '60px',
    cellLineHeight: '30px',
    cellFontSize: '15px',
}